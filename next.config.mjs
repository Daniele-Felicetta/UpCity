/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // Permetti il caricamento delle immagini dalla cartella 'public/images'
      domains: ['localhost'], // Aggiungi il tuo dominio se necessario
      // Se stai usando immagini locali, non è necessario specificare i domini
    },
  };
  
  export default nextConfig;
  
  
  
  